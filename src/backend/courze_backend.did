service: () -> {
    enrollInCourse: (text, text) -> (bool);
    getCourseProgress: (text, text) -> (opt record {refundReceived:nat; progress:float64}) query;
    updateProgress: (text, text, float64) -> (bool);
    uploadCourse: (record {id:text; title:text; progressToRefund:float64; instructor:text; price:nat}) -> (bool);
}
